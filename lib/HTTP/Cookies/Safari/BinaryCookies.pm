package HTTP::Cookies::Safari::BinaryCookies;

use strict;
use warnings;
use base qw(HTTP::Cookies);
use vars qw($VERSION);
$VERSION = '0.01';

sub load {
    die "load() is not implemented yet";
}

sub save {
    die "save() is not implemented yet";
}

1;
